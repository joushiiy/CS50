PSET4:
Introduction to pointers and how memory works.

Filter, helpers.c Implement a program that applies filters(greyscale, sepia, mirror and blur) to bitmaps (BMPs)

Recover, recovers JPEGs from a forensic image.
